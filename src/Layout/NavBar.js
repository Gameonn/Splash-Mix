import React from "react";

const navBar = (props) => {
  return (
    <div>
      <nav style={{ marginBottom: "25px" }}>
        <div className="nav-wrapper deep-purple lighten-4 left-align">
          <a href="#!" className="brand-logo">
            <i className="material-icons">leak_remove</i> SplashMix
          </a>
        </div>
      </nav>
    </div>
  );
};

export default navBar;
